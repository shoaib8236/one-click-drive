'use client'
import React, {useState} from "react";


const StyledCollapsed = (props) => {
    const {itemShow = 8} = props;

    const [items, setItems] = useState([])


    return (
        <div>

        </div>
    )
}

export default  StyledCollapsed